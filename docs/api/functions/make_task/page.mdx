# make_task

```python
def make_task(id: str, role: Literal['user', 'agent'] = 'agent', state: TaskState = TaskState.WORKING, payload: dict[str, Any] | str, session_id: Optional[str] = None) -> Task
```
